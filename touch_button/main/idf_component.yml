version: "1.0.0" # Component version, required only for components pushed to the service
targets: # List of supported targets (optional, if missing all targets are considered to be supported)
  - esp32
description: ESP-IoT-Solution Touchpad Driver # Description (optional)
url: https://github.com/iot-components/examples_touchpad.git # Original repository (optional)
dependencies:
  # Required IDF version
  idf:
    version: ">=4.0"
  touch_pad:
    git: https://github.com/iot-components/touchpad.git
    public: true
